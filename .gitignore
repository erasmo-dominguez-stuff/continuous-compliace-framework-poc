# macOS
.DS_Store

# Helm build artifacts (vendored subcharts produced by `helm dependency build`)
charts/*.tgz
charts/**/charts/*.tgz

# Local overrides / secret-bearing value files (keep out of git)
*.local.yaml
values/**/*.local.yaml
values/**/*.secret.yaml

# Built policy bundles (opa build output)
dist/
